let idade = 60;
let preco = 100;

if(idade > 60 && preco > 100){

    let precoFinal = preco - (preco * 0.20)

    console.log('Desconto Aplicado ! Novo preço: ' + precoFinal);
}

    else{
        console.log('Não foi possivel aplicar o desconto');
        
    }
